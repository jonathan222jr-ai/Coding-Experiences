# Required — Claude drives the orchestrator and all code-producing agents
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Required for run reports — Gemini authors the end-of-run report
GEMINI_API_KEY=your_gemini_api_key_here

# Only needed if you point the generated project at a ClickHouse instance
CLICKHOUSE_PASSWORD=
