# AI Training Tracker

A workout tracker that lives entirely in one HTML file — no build step, no server,
no dependencies. Open `index.html` in a browser and it works.

State persists in the browser's local storage, so a session survives a page reload
without any backend involved.

Written as an exercise in seeing how much a single self-contained file can do.
