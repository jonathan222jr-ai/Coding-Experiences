# My Office Add-in

The scaffold the [fraud email scanner](../fraud-email-scanner) grew out of — a
TypeScript Office add-in project with the manifest, build config, and task-pane
skeleton in place.

Kept as the starting point it was; the developed version is the sibling directory.

```bash
npm install
npm start
```
